__pycache__/
*.py[cod]
*.pyo
.Python
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

.venv/
venv/
env/
.conda/

.claude/
.idea/
.vscode/

.env
.env.*
!.env.example

*.log
*.tmp
.DS_Store

# Dev/prod SQLite artifacts are local — not the demo fixture.
insightsync/data/storage/insightsync.db
insightsync/data/storage/*.db-shm
insightsync/data/storage/*.db-wal
# Demo fixture is intentionally tracked so `git clone` is enough to run.
!insightsync/data/storage/demo/*.db
# Raw dumps from data sources — PDFs/CSVs are tracked when < a few MB, otherwise
# fetched via `python -m insightsync.data.cli --once`.

dist/
build/
*.egg-info/
